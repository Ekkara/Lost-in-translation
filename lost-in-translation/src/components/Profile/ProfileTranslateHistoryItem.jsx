//Just read the code. Would I honestly need to comment anything here? 
//(return single history element in form of list element to be placed in the history list)

const ProfileTranslateHistoryItem = ({ translation }) => {
    return (<li>{translation}</li>)
}
export default ProfileTranslateHistoryItem